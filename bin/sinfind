#!/bin/sh
#
# sfind
#
# Finds code-like files at or below $1, greps their contents per the
# rest of the args.
#

SCAN_ROOT="$1"
shift
GREP_ARGS="$@"

if [[ -z "$GREP_ARGS" ]]; then
    echo "no grep args"
    exit 1
else
    sfind_heart "$SCAN_ROOT" | xargs -0 grep -Hn "$GREP_ARGS"
fi
