#!/bin/sh
#
# hgrep [ARGS]*
#
# Shorthand for:
#
#   history | grep [ARGS]*
#
echo "$@"
if [ -n "$@" ]; then
    echo "A"
    history
else
    echo "B"
    history | echo "AAAA " grep "$@"
fi
echo "C"
